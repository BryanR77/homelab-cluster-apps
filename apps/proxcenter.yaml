apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: proxcenter
  namespace: argocd
spec:
  project: default
  source:
    repoURL: https://github.com/BryanR77/homelab-cluster-apps-values.git
    targetRevision: HEAD
    path: proxcenter/manifests
  destination:
    server: https://kubernetes.default.svc
    namespace: proxcenter
  syncPolicy:
    automated:
      prune: true
      selfHeal: true
    syncOptions:
      - CreateNamespace=true
  ignoreDifferences:
    - group: ""
      kind: PersistentVolumeClaim
      jsonPointers:
        - /spec/storageClassName
